{
  "name": "casino_slot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-server": "npm install --prefix backend",
    "install-client": "npm install --prefix frontend",
    "install-both": "npm run install-server && npm run install-client",
    "deploy": "npm run build --prefix backend && npm start --prefix backend",
    "server": "npm run watch --prefix backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
